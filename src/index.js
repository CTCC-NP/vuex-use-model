import { createORM, installORM } from './plugin'
import Model from './model'
import Collection from './collection'
import { useModel } from './helper'

export {
  createORM,
  installORM,
  Model,
  Collection,
  useModel,
}
